{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "generate",
            "command": "pnpm",
            "args": ["run", "generate"],
            "options": {
                "cwd": "${workspaceFolder}/core"
            },
            "group": "build",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
        }
    ]
}
