---
"@bigcommerce/catalyst-core": patch
---

Update with-routes logic to ensure public assets are routed to localized paths. Make sure to remove your locale `.next` directory before running the build/dev mode.
