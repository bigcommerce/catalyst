"use client"; // Marks this file as a client-side component

import React from "react";
// import { Page as MakeswiftPage } from "~/lib/makeswift"; // Ensure the path is correct

const MakeswiftContent: React.FC = () => { // Add the correct function syntax and type
  return (
    <div>
      {/* Render Makeswift content dynamically */}
      {/* <MakeswiftPage locale="en" path="/content/returns" /> */}
      <p>ki</p>
    </div>
  );
};

export default MakeswiftContent;
